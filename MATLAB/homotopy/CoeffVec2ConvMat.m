%  Generating the convolution matrix C_k(f) such that 
%       C_k(f)*g = conv(f,g), for g of length k 
%
%   Input:  f -- a coefficient vector 
%           k -- the length of g 
%
%  syntax  >> C = CoeffVec2ConvMat(f,k) 
